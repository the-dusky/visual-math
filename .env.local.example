# Upstash Redis (optional — without these, My Story uses localStorage only)
# Get these from https://console.upstash.com after creating a Redis database
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
